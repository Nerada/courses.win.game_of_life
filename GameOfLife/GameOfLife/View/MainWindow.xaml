<Window x:Class="GameOfLife.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:GameOfLife.ViewModels"
        xmlns:view="clr-namespace:GameOfLife.View"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModels:MapViewModel}"
        Title="Game of life" SizeToContent="WidthAndHeight" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <TextBlock Margin="10, 0,10,0">
                <Hyperlink RequestNavigate="Hyperlink_RequestNavigate" NavigateUri="{Binding PatternUri}">
                    <Run Text="{Binding PatternName, Mode=OneWay}" />
                </Hyperlink>
            </TextBlock>
        </StackPanel>
        <view:ViewerUserControl Grid.Row="1" DataContext="{Binding Mode=OneWay}" Margin="10" HorizontalAlignment="Left"
                                VerticalAlignment="Top" />
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Height="50" Width="100" Command="{Binding IncreaseIteration}" HorizontalAlignment="Left"
                    Content="Add iteration" Margin="10,0,10,10" />
            <Button Height="50" Width="100" Command="{Binding ToggleAutoRun}" HorizontalAlignment="Left"
                    Content="Toggle Autorun" Margin="10,0,10,10" />
        </StackPanel>
    </Grid>
</Window>